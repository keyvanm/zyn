[package]
name = "zyn-probe"
version = "0.1.0"
edition = "2021"
description = "Fast session-existence check for zyn"

[dependencies]
clap = { version = "4", features = ["derive"] }
md-5 = "0.10"
hex = "0.4"

[profile.release]
lto = true
codegen-units = 1
strip = true
